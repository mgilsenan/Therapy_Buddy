<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.therapybuddy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TherapyBuddy">

        <activity android:name=".MoodLogDisplayActivity"/>
        <activity android:name=".MoodLogActivity" />
        <activity
            android:name=".ThoughtRecordActivity"
            android:label="@string/title_activity_thought_record"></activity>
        <activity android:name=".ThoughtRecordStatisticsActivity" />
        <activity android:name=".WorkSheetsActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity
            android:name=".StatisticsActivity"
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>